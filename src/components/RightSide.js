import React from 'react';
import style from '../css_modules/RightSide.module.css'

const RightSide = () => {
    return (
        <div className={`${style.main} `}>

        </div>
    );
};

export default RightSide;